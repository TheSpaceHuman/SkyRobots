// $(document).ready(function () {
//   Toastr.success('Сообщение успешно отправлено', 'Успех');
// })


/*
const indexForm = $('#index-form');
console.log('indexForm', indexForm);
indexForm.find('.btn').click(function (e) {
  e.preventDefault();
  let formData =  {
    name: indexForm.find('input[name=name]').val()
  }
  console.log(JSON.stringify(formData));
  $.ajax({
    url: indexForm.attr('action'),
    data: 'dsa',
    success: function () {
      toastr.success('Сообщение успешно отправлено', 'Успех');
    },
    error: function () {
      toastr.error('Что-то пошло не так', 'Ошибка');
    }
  })
});
*/
